import React from 'react';
import styles from './index.module.scss';

const Banner: React.FC = () => {
  return <div className={`${styles.painPointsContainer} container`}>hello world</div>;
};

export default Banner;
